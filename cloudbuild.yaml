steps:
  # Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/gcp-praticals/aryan-dot-net/web:latest', '.']

  # Push
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/gcp-praticals/aryan-dot-net/web:latest']

  	  # Deploy to Cloud Run--name-> CLI goodle cloud sdk kit 
	  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
	    entrypoint: 'gcloud'
	    args:
	      - run
	      - deploy
	      - assessment
	      - '--image'
	      - 'us-central1-docker.pkg.dev/cloud-run-tutorial-424007/dotnet-api-artiii/web:latest'
	      - '--region'
	      - us-central1
	options:
	  logging: CLOUD_LOGGING_ONLY


options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _COMMIT_SHA: $COMMIT_SHA
  _BUILD_ID: $BUILD_ID
